rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /patients/{patientId} {
      allow read: if request.auth != null &&
                  request.auth.token.hospital_id in resource.data.assignedHospitals;
    }
  }
}
